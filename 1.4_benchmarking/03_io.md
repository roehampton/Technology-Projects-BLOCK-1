# 🧪 Task 3 — Storage Read/Write Speed

### 1️⃣ Write test:
```bash
dd if=/dev/zero of=testfile.bin bs=100M count=1 oflag=direct
```

### 2️⃣ Read test:
```bash
dd if=testfile.bin of=/dev/null bs=100M count=1 iflag=direct
```

### 3️⃣ Record:
- Write speed
- Read speed

📁 Save results in `results/io.txt`
